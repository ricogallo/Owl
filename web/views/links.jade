extends layout

include mixins/linkEntry

block content
  script(src='/js/tour.js')
  include createLink
  if links && links.length
    //= links.indexOf(2)
    for link in links
      if link
        = linkEntry_mixin(link, user, (links.length - 1) == links.indexOf(link))
